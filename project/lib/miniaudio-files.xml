<xml>

    <files id="native-toolkit-miniaudio-depends">
        <depend name="${NATIVE_TOOLKIT_PATH}/miniaudio/miniaudio.h" />
    </files>

    <files id="native-toolkit-miniaudio" tags="">
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/miniaudio/" />
        <cache value="1" />

        <tag value="${NATIVE_TOOLKIT_OPTIM_TAG}" if="NATIVE_TOOLKIT_OPTIM_TAG" />

        <depend files="native-toolkit-miniaudio-depends" />

        <file name="${NATIVE_TOOLKIT_PATH}/miniaudio/miniaudio.c" />

    </files>

</xml>
